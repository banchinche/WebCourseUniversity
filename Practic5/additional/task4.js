const calculate = (first, second, third) => (first + second + third) / 3

const first = 40, second = 50, third = 60;
console.log('Testing method with such values');
console.log('first:', first, 'second:', second, 'third:', third);
console.log('Calculate method result:', calculate(first, second, third));
